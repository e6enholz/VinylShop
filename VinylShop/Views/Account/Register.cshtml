@model VinylShop.Models.User
@{
    ViewData["Title"] = "Регистрация";
}
<div class="row justify-content-center mt-5">
    <div class="col-md-4">
        <div class="card shadow border-0">
            <div class="card-header bg-dark text-warning text-center fw-bold text-uppercase">📝 Регистрация</div>
            <div class="card-body p-4">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger p-2 small">@ViewBag.Error</div>
                }
                <form asp-action="Register" method="post">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Логин</label>
                        <input asp-for="login" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Email</label>
                        <input asp-for="email" type="email" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Пароль</label>
                        <input asp-for="password" type="password" class="form-control" required />
                    </div>
                    <button type="submit" class="btn btn-dark text-warning w-100 fw-bold">СОЗДАТЬ АККАУНТ</button>
                </form>
                <div class="text-center mt-3">
                    <a asp-action="Login" class="small text-muted">Уже есть аккаунт? Войти</a>
                </div>
            </div>
        </div>
    </div>
</div>